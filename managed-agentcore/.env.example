# AWS Region
AWS_REGION=us-east-1

# Default developer name (can be overridden via --dev_name)
DEV_NAME=sejong

# AgentCore Runtime (populated by 01_create_agentcore_runtime.py)
RUNTIME_NAME=
RUNTIME_ARN=
RUNTIME_ID=
